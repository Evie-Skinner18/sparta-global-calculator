var which-calculator = prompt("Which calculator would you like to use? Type B for basic or A for advanced.");
if (which-calculator == "B"){
  // get the user inputs
  var value1 = prompt('enter first value');
  var value2 = prompt('enter the second value');
  var choice = prompt('Choose your action (a)dd (s)ubtract (m)ultiply (d)ivide (%)modulus') || "a";


}
